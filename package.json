{
  "name": "ed-moveis",
  "version": "1.0.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate",
    "start": "cd backend && npx prisma migrate resolve --applied 20260524182825_postgres_init; npx prisma migrate deploy; node server.js"
  }
}
